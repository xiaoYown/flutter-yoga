# Flutter + iOS/Android 跨平台项目规则（.cursorrules）

## 通用规则（General Rules）
- 保持规则简洁、明确，强调代码而非空话。
- 项目需支持 Flutter 与原生（iOS/Android）SDK 的分离式构建与发布。
- 使用 Melos 或其他方案管理多包结构（Flutter + Native 包分离）。
- 原生 SDK（iOS/Android）应可独立编译并发布为平台库。
- 所有原生平台调用统一封装在 `platform_bridge` 中，不允许 Flutter 直接调用原生代码。
- 保持各平台代码风格一致：Swift 遵循 iOS 官方规范，Kotlin 遵循 Android Jetpack 最佳实践。
- 所有公共接口需使用抽象定义，平台差异在内部隐藏。
- Flutter 与原生之间通信需经过完整封装与错误处理，避免平台耦合。
- Flutter 与原生模块均需具备独立 CI/CD 构建流程。

## Flutter 最佳实践（Flutter Best Practices）
- 使用 Flutter 3.x 与 Material 3 设计系统。
- 使用 BLoC 模式实现 Clean Architecture。
- 遵循 null safety 安全规范。
- 功能模块按特性组织，分层明确（data/domain/presentation）。
- 使用 GoRouter 管理导航。
- 依赖注入使用 GetIt。
- 错误处理必须统一、规范。
- 国际化使用 `intl` 与自动生成的 `l10n`。

## 项目结构建议（Project Structure）
- lib/
  - core/
    - constants/
    - theme/
    - utils/
    - widgets/
  - features/
    - feature_name/
      - data/
        - datasources/
        - models/
        - repositories/
      - domain/
        - entities/
        - repositories/
        - usecases/
      - presentation/
        - bloc/
        - pages/
        - widgets/
  - l10n/
  - main.dart
- test/
  - unit/
  - widget/
  - integration/
- native/
  - ios_sdk/
  - android_sdk/


## 编码规范（Coding Guidelines）
- 强制启用 null safety。
- 使用 Either 类型处理异常。
- 遵循统一命名规范（lower_snake_case、CamelCase 等）。
- Widget 粒度小、职责单一。
- 表单验证统一封装。
- 所有页面状态管理均使用 BLoC。
- 路由与依赖注入逻辑不可混杂。
- 资源（图片、字体等）集中管理。
- 单元测试、集成测试齐全。

## Widget 编写规范（Widget Guidelines）
- 使用 `const` 构造函数优化性能。
- 合理使用 `Key` 提高组件复用。
- 避免嵌套过深的布局。
- 使用生命周期钩子管理资源。
- 页面组件之间解耦。
- 支持无障碍（Accessibility）。

## 性能优化（Performance Guidelines）
- 图片统一缓存策略。
- 列表使用懒加载方式（ListView.builder）。
- 减少无效 rebuild，使用 `BlocBuilder` 精准监听。
- 内存释放：控制器、监听器需手动清理。
- Platform Channel 使用需控制频率。
- Release 构建开启 Dart 压缩与混淆。

## 测试规范（Testing Guidelines）
- 业务逻辑必须具备单元测试。
- UI 层 Widget 测试覆盖交互行为。
- 集成测试模拟真实使用流程。
- 使用 Mockito、Mocktail 等进行依赖隔离。
- 集成 CI/CD 自动化测试流程。
- 使用 lcov 等工具检查覆盖率。
- 测试命名规范：行为_预期结果。

## 原生模块要求（iOS/Android SDK）
- 每个平台应提供独立 `native/ios_sdk` 与 `native/android_sdk` 目录。
- iOS SDK 使用 Swift 编写，支持 CocoaPods/SPM。
- Android SDK 使用 Kotlin 编写，支持 Maven/Gradle 发布。
- Flutter 调用原生逻辑只能通过统一的 PlatformBridge。
- 所有对外接口需文档、单元测试与错误码定义。
- SDK 可用于独立原生项目，不依赖 Flutter。

## 构建与交付（Build & Release）
- Flutter 与原生模块需分别维护 CI/CD 流程。
- 发布包需支持内部测试与生产环境切换。
- 所有包需标注版本号与变更日志。
- 构建脚本需兼容 macOS 与 CI 环境。

## 安全与合规（Security & Compliance）
- 敏感数据不得明文存储（使用加密插件）。
- 原生与 Flutter 的所有通信需进行错误边界检查。
- 发布包需遵守平台隐私政策（如 IDFA、权限声明等）。

